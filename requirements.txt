# Runtime dependencies (what the Raspberry Pi / VM needs).
# Development/test extras live in requirements-dev.txt.
python-telegram-bot[job-queue]>=21.0
qrcode[pil]>=7.4
python-dotenv>=1.0
google-genai>=2.9,<3.0
gspread>=6.0
